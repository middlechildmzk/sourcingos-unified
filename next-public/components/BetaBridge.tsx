'use client'
import { useState } from 'react'
export function BetaBridge(){ const [pass,setPass]=useState(''); const [ok,setOk]=useState(false); if(!ok) return <div className="interactive-tool"><h2>Private beta gate</h2><p className="muted">SourcingOS Core stays gated while the public Vault grows. Local preview passphrase: <code>sourcingos-beta</code>.</p><input className="input" value={pass} onChange={e=>setPass(e.target.value)} placeholder="Passphrase"/><button className="btn" onClick={()=>setOk(pass==='sourcingos-beta')}>Unlock preview</button></div>; return <div className="interactive-tool"><h2>SourcingOS Core bridge</h2><p>The Vite Core cockpit remains the protected private app: Candidate 360, Evidence Matrix, connectors, synthesis, project memory, feedback events, and rediscovery.</p><p className="cta">Deployment plan: mount the existing Vite app at <code>/app</code> or <code>app.sourcingos.com</code>. Keep the public SEO shell separate.</p><ul><li>No auto-merge.</li><li>No automated outreach.</li><li>Clearance mentions are breadcrumbs only.</li><li>Human approval required for every state-changing action.</li></ul></div> }
