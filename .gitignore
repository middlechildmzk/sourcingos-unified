# SourcingOS build/dependency artifacts
next-public/.next/
next-public/node_modules/
next-public/tsconfig.tsbuildinfo

# Local/deployment secrets — ignore every environment file except the intentionally
# blank documented example. Never commit provider credentials or service-role keys.
.env*
!next-public/.env.example
!.env.example
.vercel/
*.pem
*.key
*.p12
*.pfx
next-public/v40-5i-admission-report.json
